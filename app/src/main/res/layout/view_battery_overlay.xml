<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#B3000000"> <!-- 70% Black Dim -->

    <!-- 1. The Border Layer (Assistant Style) -->
    <!-- Uses bg_thick_border drawable -->
    <View
        android:id="@+id/borderView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_thick_border" />

    <!-- 2. The Flash Layer -->
    <View
        android:id="@+id/whiteFlashOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFFFFF"
        android:alpha="0" />

    <!-- 3. Center Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="BATTERY CRITICAL"
            android:textColor="@color/white"
            android:textSize="24sp"
            android:letterSpacing="0.2"
            android:textStyle="bold"
            android:shadowColor="#000000"
            android:shadowDx="4"
            android:shadowDy="4"
            android:shadowRadius="8"/>

        <TextView
            android:id="@+id/tvPercentage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="15%"
            android:textColor="#FF0000"
            android:textSize="96sp"
            android:textStyle="bold"
            android:includeFontPadding="false"
            android:shadowColor="#000000"
            android:shadowDx="10"
            android:shadowDy="10"
            android:shadowRadius="10"/>

    </LinearLayout>

    <!-- 4. Bottom Dismiss Area -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDismiss"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_gravity="bottom"
        android:layout_margin="32dp"
        android:text="I ACKNOWLEDGE"
        android:textSize="18sp"
        android:textStyle="bold"
        app:cornerRadius="4dp"
        android:backgroundTint="@color/white"
        android:textColor="@color/black"/>

</FrameLayout>